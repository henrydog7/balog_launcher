import 'package:flutter/material.dart';
import 'package:intl/intl.dart';

void main() {
  runApp(BalogLauncher());
}

class BalogLauncher extends StatelessWidget {
  const BalogLauncher({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

